import React, { useState } from 'react';
import { NavProps } from '../../containers/Nav';

export const Nav = ({}: NavProps): JSX.Element => {
  return (
    <div>
      <div>
        <div>어디갔어 props</div>
      </div>
    </div>
  );
};
