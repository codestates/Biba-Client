import React from 'react';
import styled from 'styled-components';

import { ModalProps } from '../../containers/nav/Modal';

export const Modal = ({ content }: ModalProps): JSX.Element => {
  return <>{content}</>;
};
